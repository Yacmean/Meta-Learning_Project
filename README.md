Here you can find the code and report of my Master's project at the University of Ottawa.

In My Master's project, the goal was to experiment with several k-nearest neighbor (knn) models as self-training models in an online setting in order to understand how well the knn models are able to assign labels to the unlabeled set of data using a small set of labeled data.
